/**
 * Prefix for timeline `Event.note` when a recurring responsibility is completed.
 * Must stay in sync with `ResponsibilityService.markComplete` note text.
 */
export const RECURRING_COMPLETION_NOTE_PREFIX = 'Recurring completion';
